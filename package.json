{
  "name": "centralwebdesk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "cf:setup": "node scripts/cloudflare-setup.js",
    "cf:d1:migrate": "node scripts/d1-migrate.js",
    "cf:d1:generate": "drizzle-kit generate"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@sveltejs/adapter-cloudflare": "^6.0.0",
    "@sveltejs/kit": "^2.20.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.2.0",
    "wrangler": "^4.0.0",
    "drizzle-kit": "^0.30.0"
  },
  "dependencies": {
    "@clerk/sveltekit": "^0.80.0",
    "@floating-ui/dom": "^1.6.0",
    "drizzle-orm": "^0.40.0"
  },
  "type": "module"
}
